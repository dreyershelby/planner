/* planner.jsx written by bea dreyer */function main() {  // open the planner document, not showing the window  // (faster compiling)  var doc = app.open(File("/Users/juniperdreyer/Library/Mobile\ Documents/"                          + "com\~apple\~CloudDocs/mental-health/planner/"                          + "default.indb"), true);  // the very base of the planner is the master default, aka dflt-master  // this contains only the page number footers & the 6 hole punches for  // each page  // the page number automatically works just fine, so all we need to  // update (occasionally) are the hole punches  var master = doc.masterSpreads.item("dflt-master");  // constants  const hole_diameter = '0.25i'; // quarter of an inch  const distance_btwn_holes /* in a subgroup of 3 */ = '0.5i';  const distance_btwn_hole_grps /* in a subgroup of 2 subgroups */                                = '2.5i';  // do we need to change anything about the holes we already have down?  const update_holes = true;  // how do we get the date?  // var today = $.buildDate(); // returns a Date object  // returns one of {Sunday, ..., Saturday} but as numbers 0-6  // var day_of_week = today.getDay();}main();